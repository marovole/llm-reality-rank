export const defaultDescription = 'LLM Reality Rank 是面向中文高阶 AI 用户的静态模型价值榜，展示 reviewed alpha 数据、置信度、来源证据与限制说明。';

export function pageTitle(title: string): string {
  return `${title} | LLM Reality Rank`;
}
